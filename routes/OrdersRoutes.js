const router = require('express').Router();

//create a order
router.post('/',()=>{})

//get a order
router.get('/',()=>{})

//get multiple orders
router.post('/multiple',()=>{})

//Update Single orders
router.post('/multiple',()=>{})

module.exports = router;